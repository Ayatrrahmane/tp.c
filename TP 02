   #include<stdoio.h>
   #include<stdlib>
   #include<string>
1-
char *chargerchaine (int N)
char  *chaine =(char*
  gets( chaine )
if(chaine==NULL)
  printf("erreur";\ n)
retuirn 1;
 printf ("veuillez saisir une chaine de caracteres :",N)
 scanf ("%c \n",chaine);
return chaine;
2-
int longueur(char *ch)
 int i=0;
  while(chaine [i]!= 0)
  i++;
return i;
3-
void charger Tab(char *p,char Tab [])
 for (int i=0;i<p;i++)
  Tab[i]=p[i];
  Tab[p]='\0';
4-
void inverser TAB(char Tab[])
int m;
{
for (i=0;i<m;i++)
 T[i]=Tab[m-i-1];
 T[m]='\0';
}
5-
void afficherTab(char Tab[], int m;
for (inti=0;i<m; i++)
  printf("%c",Tab[i]);
  printf("\n");

 int main(){
int N;
printf("entrez la taille de la chaine :");
scanf ("%d",&N);

char *chaine=chargerchaine(N);
 int longueur=longueur(chaine);
char Tab[N],T[N];
chargerTab(chaine,Tab);

inverseTab(Tab,T,longueur);
printf("la chaine inverser est :");

afficherTab(t,longueur);
free(chaine);
return 0;



